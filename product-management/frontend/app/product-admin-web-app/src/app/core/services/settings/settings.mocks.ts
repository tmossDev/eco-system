import { ApplicationSettings } from './settings.models';

export const MOCK_SETTINGS: ApplicationSettings = {
  applicationName: 'Product Admin Web App',
  defaultCategory: 'General',
  inventoryAlerts: true,
  requireReview: false,
  defaultCurrencyCode: 'USD',
};
