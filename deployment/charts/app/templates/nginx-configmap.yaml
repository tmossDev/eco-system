{{- if and .Values.nginxConfig.enabled (not .Values.nginxConfig.existingName) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.nginxConfigName" . }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.nginxConfig.data | nindent 2 }}
{{- end }}
