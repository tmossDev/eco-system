{
  "name": "design-system",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "build": "pnpm build:button && pnpm build:icon && pnpm build:header",
    "build:button": "ng build button",
    "build:icon": "ng build icon",
    "build:header": "ng build header",
    "watch:button": "ng build button --watch --configuration development",
    "watch:icon": "ng build icon --watch --configuration development",
    "watch:header": "ng build header --watch --configuration development",
    "test": "ng test"
  },
  "private": true,
  "packageManager": "pnpm@11.0.9",
  "dependencies": {
    "@angular/common": "catalog:",
    "@angular/compiler": "catalog:",
    "@angular/core": "catalog:",
    "@angular/forms": "catalog:",
    "@angular/platform-browser": "catalog:",
    "@angular/router": "catalog:",
    "@storybook/angular": "catalog:",
    "rxjs": "catalog:",
    "tslib": "catalog:"
  },
  "devDependencies": {
    "@angular/build": "catalog:",
    "@angular/cli": "catalog:",
    "@angular/compiler-cli": "catalog:",
    "jsdom": "catalog:",
    "ng-packagr": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
