# @primitive/form

Reusable Angular form primitives for accessible, consistently styled native controls.

Use `DsFormField` with `primitiveInput` or `primitiveSelect` to connect labels, hints, errors, and ARIA descriptions automatically. Use `primitiveCheckbox` for standalone checkbox styling that still works with Angular forms.
